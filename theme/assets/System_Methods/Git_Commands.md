# Update Local from GitHub
cd C:\_Data\Mybizz\Mybizz_core_v1_2_dev
git pull origin master

# to GitHub from VS Code
C:\_Data\Mybizz\Mybizz_core_v1_2_dev
git add .                    # Stage all changes
git commit -m "Your message" # Commit with message  
git push origin master       # Push to Anvil → GitHub sync

# Essential Git Commands 
git status              # Check what's changed/ready
git log --oneline   # See commit history
git diff            # Preview changes before commit

