All five files parse cleanly. Here is the complete review report.

---

## Stage 1.2 — Auth Code Review Report

**All 5 files: syntax ✅ | 14 checks completed**

---

### Fixes Applied

#### `ui_helpers.py` — 4 fixes
| # | Issue | Fix |
|---|---|---|
| 1 | Module docstring placed **after** `import` statements — Python ignores it as a docstring | Moved docstring to line 1 |
| 2 | `import anvil.server` present but never used in this client module | Removed |
| 3 | Emoji literals in `format_role_display` and `format_account_status` were corrupted (encoding artefacts from original file) | Replaced with plain text labels |
| 4 | `from datetime import timedelta` imported inside `format_last_login` but `timedelta` was never used there | Removed unused import |

#### `LoginForm/__init__.py` — 3 fixes
| # | Issue | Fix |
|---|---|---|
| 5 | `import logging` + `logger = logging.getLogger(__name__)` present but logger never used — client modules don't do structured logging | Removed both |
| 6 | `anvil.server.TimeoutError` not caught — a timeout would fall through to the bare `Exception` handler with a misleading message | Added explicit `TimeoutError` handler |
| 7 | `Notification("Please correct the highlighted fields.")` shown before field errors were set, then a second notification shown on server failure — double-notification pattern | Removed the pre-validation notification; errors now shown only once via field `error=True` + single server-failure notification |

#### `SignupForm/__init__.py` — 4 fixes
| # | Issue | Fix |
|---|---|---|
| 8 | **Critical syntax error** — `is_strong, message = ...` and `confirm_password = ...` had leading whitespace that broke indentation inside `_handle_create_account` | Fixed indentation |
| 9 | `import logging` + `logger = logging.getLogger(__name__)` — unused in client module | Removed both |
| 10 | `anvil.server.TimeoutError` not caught | Added explicit handler |
| 11 | `link_sign_in`, `link_terms`, `link_privacy` components referenced in handlers but absent from `1.2-ui-design.yaml` component list | Added `⚠️ NEEDS HUMAN REVIEW` note in docstring |

#### `PasswordResetForm/__init__.py` — 1 fix
| # | Issue | Fix |
|---|---|---|
| 12 | `link_back_to_sign_in` component referenced in handler but absent from `1.2-ui-design.yaml` component list | Added `⚠️ NEEDS HUMAN REVIEW` note in docstring |

#### `service.py` — 5 fixes
| # | Issue | Fix |
|---|---|---|
| 13 | Scaffold imports from Anvil template (`anvil.google.auth`, `anvil.google.drive`, `anvil.google.mail`, `anvil.files`, `anvil.email`, `app_files`, `data_files`) — none used in this module | Removed all 6 unused imports |
| 14 | `anvil.users.TooManyPasswordFailures` not caught explicitly — it is a subclass of `AuthenticationFailed` but the user message should be specific (account locked, must reset via email) | Added explicit handler before `AuthenticationFailed` with correct message |
| 15 | No `logger.info` at entry of any callable — violates ref_anvil_coding.md §3 observability requirement | Added `logger.info("... called")` as first line of all 5 callables |
| 16 | `anvil.users.get_user_by_email()` used without confirmation — context7 did not return a confirmed signature for this function | Wrapped in `try/except AttributeError` with `⚠️ NEEDS HUMAN REVIEW` note; `UserExists` from `signup_with_email` still catches duplicates as fallback |
| 17 | Generic `except Exception as err: return {'success': False, 'error': str(err)}` in callables — leaks internal exception messages to client | Changed to `except Exception:` with `logger.error(..., exc_info=True)` and a safe generic user-facing message |

---

### Items Requiring Human Action

These are not code bugs — they require a decision or a Designer action:

| # | File | Issue |
|---|---|---|
| A | `SignupForm/__init__.py` | `link_sign_in`, `link_terms`, `link_privacy` handlers exist but these components are not in `1.2-ui-design.yaml`. Add them in the Designer, or remove the handlers if they are not needed. |
| B | `PasswordResetForm/__init__.py` | `link_back_to_sign_in` handler exists but the component is not in `1.2-ui-design.yaml`. Same decision required. |
| C | `service.py` | `anvil.users.get_user_by_email()` — verify this function exists in the Anvil runtime version in use. If it does not, the `AttributeError` guard will silently skip the pre-check and `UserExists` will catch duplicates instead. |
| D | All 3 auth forms | `txt_password.type = 'password'` — the M3 TextBox `type` property was flagged `⚠️ AMBIGUOUS` in `1.2-ui-design.yaml`. Human developer must set `type = password` in the Designer Properties Panel for both password fields. |
| E | All 3 auth forms | Write-back bindings (`W` toggle) for all input components must be enabled in the Designer Properties Panel — this cannot be done in code. |